DEBUG=True
SECRET_KEY=change-me-in-dev
DATABASE_URL=postgres://salon_user:salon_pass@localhost:5432/salon_dev
ALLOWED_HOSTS=localhost,127.0.0.1
TIME_ZONE=UTC

# Only needed if frontend/backend are split across separate domains
# (not the case in local dev, which uses the Vite proxy for same-origin requests)
CORS_ALLOWED_ORIGINS=
CSRF_TRUSTED_ORIGINS=
